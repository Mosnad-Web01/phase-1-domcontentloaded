// Your code goes here
document.addEventListener("DOMContentLoaded", function () {
  // Get the element that contains the text
  const textElement = document.getElementById("text"); // Assuming the element has an id of 'text'

  // Change its text content
  textElement.textContent = "This is really cool!";
});
